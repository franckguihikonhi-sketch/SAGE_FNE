{
  "name": "sage-fne",
  "version": "0.1.0",
  "private": true,
  "description": "Transforme les factures certifiees FNE (DGI Cote d'Ivoire) en fichier d'import Sage 100 Gestion Commerciale.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsx scripts/construire-page.ts",
    "verify": "node scripts/verifier-page.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.0",
    "playwright": "^1.55.0",
    "tsx": "^4.23.12",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
